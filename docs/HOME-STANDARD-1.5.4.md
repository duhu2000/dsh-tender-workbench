# DSH-UX-001 1.5.4 首页采用记录

2026-09-14；基线 main ee9d7793d0c1e3405824a25143a7c2f307588ea1（远端只读回读一致），本轮不升版、不发布、不合并。

- 首页只保留图标与“招投标智能体”大标题，删除副标题 DOM 及其 CSS，没有空段落占位。原生 workspace/模式、输入框与提示词生成保持；快捷菜单仍在输入框之后。
- UX-45：左侧名称保持“招投标”，创建中用 disabled/aria-busy，失败使用相邻错误，不改名；业务阶段与“任务历史”保持现有名称。
- UX-46：业务以专属 Session ID 判断，useSession 订阅 blank/running/promptAttempted；桥接只观察当前 Hero，不观察全局 document.body。支持原生标题延迟出现及重建，清理观察器/自有节点，并仅在 display 仍是本桥接值时恢复。没有写入用户 Session 标题或草稿，也不改其他产品标题。未知 DOM 不替换。
- UX-47：复用 0.5.8 已有真实事件进度/历史恢复，未新增首页状态或计时假进度；现有执行状态为 running/succeeded/partial/interrupted/failed，等待人工确认在业务流程呈现。未把短暂无结果认定完成。新的统一“同步异常/已停止但未完成”语义与真实 Host 终止/重连组合还需真实宿主验收，本轮不能宣称该项全覆盖。

## 验证

- 正确 0.1.2-rc.1 隔离依赖下 typecheck 通过；入口、历史和进度 3 文件 28 测试通过。
- Chromium React fixture：浅/深色 × 1440×900、1024×768、390×700、900×500 共 8 场景通过。含副标题不存在、标题→workspace/模式→输入框→快捷菜单几何顺序、普通会话退出恢复、键盘交互。
- 新测试覆盖标题延迟装载、替换后只保留一份品牌、恢复自定义原生文字、保留草稿；未知宿主 DOM 保持。
- 本轮未验证真实 DSH 最终 tarball、四产品同装、真实模型/MCP；UI fixture 不是实际宿主证据。
- 截图位于本工作树 `_scratch/ui-blue/`。生产 ~/.dsh、全局宿主和 3080 未改动。

## 本地环境

原始工作树 node_modules 与当前代码版本不一致，初次类型检查因旧 RC 缺模块失败；验证改用已有隔离 0.5.8 工作树依赖，未修改原仓依赖。Chromium 初次沙箱启动受系统 MachPort 限制，升级执行权限后 8 场景通过。
